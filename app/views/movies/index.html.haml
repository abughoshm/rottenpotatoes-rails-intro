-# This file is app/views/movies/index.html.haml %h2 All Movies
= form_tag movies_path, :method => :get, id: "ratings_form" do Include:
- @all_ratings.each do |rating| = rating
= check_box_tag "ratings[#{rating}]", 1, @current_ratings.nil? ? true : @current_ratings.include?(rating), id: "ratings_#{rating = submit_tag 'Refresh', :class => 'btn btn-primary', id: "ratings_submit"
%table#movies.table.table-striped.col-md-12 %thead
%tr
%th{:class => @current_sorting != 'title' ? ' ': ['bg-warning', 'hilite']}=link_to 'Movie Title', movies_path(:sorting => 'tit %th Rating
%th{:class => @current_sorting != 'release_date' ? ' ' : ['bg-warning', 'hilite']}=link_to 'Release Date', movies_path(:sortin %th More Info
%tbody
- @movies.each do |movie|
%tr
%td= movie.title
%td= movie.rating
%td= movie.release_date
%td= link_to "More about #{movie.title}", movie_path(movie)
= link_to 'Add new movie', new_movie_path